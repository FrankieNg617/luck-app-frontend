import 'package:flutter/material.dart';
import '../background/ready_background.dart';

class ReadyScreen extends StatelessWidget {
  const ReadyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
      body: ReadyBackground(
        
        
      ),
    );
  }
}
